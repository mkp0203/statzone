import React from "react";

const styles = {
    title: {
        fontHeight: '40px',
        color: "#feda6a",
    }
}

const WebTitle = () => (
    <h1 style={styles.title}>Stat Zone</h1>
)

export default WebTitle;